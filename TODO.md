# TODO

这份 TODO 只保留**还没实现**、但和课题直接相关的内容。

当前原则：

- 现有数据里已经有的内容都要真正用上
- 不再区分“第一版 / 第二版”
- 不补保底代码，不补兼容层，不补冗余分支
- 按课题当前真实方向推进：**空间 + 意义 + 身份**
- 卫星图已在数据中存在，当前直接作为普通空间图像输入，不单开遥感分支

---

## 一、离“直接开始训练”还差什么

### 1. 跑通单区域训练主链路

- [ ] 跑通 `kaitong_west_lane` 首轮训练
  目标：
  - 训练命令可直接启动
  - 能稳定产出 checkpoint
  - 当前数据四类输入全部参与到应参与的流程里

- [ ] 跑通单区域评估与导出
  目标：
  - `evaluate`
  - `export-region-metrics`
  - `export-visualizations`
  - 生成可以直接看结果的输出目录

- [ ] 补 1 个最小端到端 smoke test
  位置：
  - `tests/`
  目标：
  - 覆盖 `manifest -> dataloader -> model -> trainer`
  - 覆盖 YAML 配置加载与 `runs/train/<experiment_name>/` 输出目录
  - 只验证主链路，不做额外工程兼容

---

## 二、单区域结果还缺什么

### 1. 结果可解释

- [ ] 增加启动时数据摘要日志
  位置：
  - `src/uniter/training/trainer.py`
  目标：
  - 打印 train/val/test 样本数
  - 打印图像数、当前文本数、历史文本数、身份文本数
  - 明确当前 run 实际吃到了哪些数据

- [ ] 增加单区域结果总表
  位置：
  - `src/uniter/reporting/`
  目标：
  - 汇总 `IFI / MDI / IAI / alignment_gap / final level`
  - 直接服务案例分析，不额外做一套复杂报告系统

### 2. 结果是否符合课题解释逻辑

- [ ] 检查单区域输出是否能解释
  目标：
  - `IFI` 是否和空间图像表现一致
  - `MDI` 是否和当前/历史文本差异一致
  - `IAI` 是否能反映身份文本提供的场所角色
  - `alignment_gap` 是否只是辅助，不喧宾夺主

---

## 三、按当前单区域课题方向还没完成的核心项

### 1. `MDI` 实验口径最终收口

- [ ] 明确 `MDI` 的正式实验口径
  目标：
  - 明确什么时候用情感漂移
  - 明确什么时候退回嵌入漂移
  - 不保留模糊表述

### 2. 身份模态完成正式化

- [ ] 增加 identity-on 的端到端测试
  目标：
  - 覆盖 identity 开启时的数据读取、前向、指标和导出

### 3. 分割监督是否接入

- [ ] 明确空间分支是否接分割监督
  当前状态：
  - 数据里还没有 mask
  处理原则：
  - 如果后续不补 mask，就不要继续扩展这条线
  - 如果补 mask，再把 `spatial_supervision` 真正接上

### 4. 阈值校准实验闭环

- [ ] 用真实多区域样本做阈值标定
  目标：
  - 给 `IFI / MDI / IAI` 的轻中重阈值一个真实样本基线
  - 当前阶段至少先导出单区域校准结果占位

- [ ] 补统一实验输出规范
  目标：
  - 所有训练、评估、导出、可视化结果统一落到 `runs/train/<experiment_name>/`
  - 以 `resolved_config.yaml` 固化实验配置
  - 不再使用散乱输出目录

- [ ] 补一键复现实验脚本
  目标：
  - 能直接跑某个 YAML 实验配置
  - 自动导出对应结果

---

## 四、执行顺序

### Phase 0：先达到单区域可训练

- [ ] 跑通首轮训练
- [ ] 跑通评估与导出
- [ ] 补最小 smoke test

### Phase 1：把单区域结果做成可分析样本

- [ ] 增加启动数据摘要
- [ ] 生成单区域结果总表
- [ ] 检查 `IFI / MDI / IAI / alignment_gap`
- [ ] 确认单区域结果是否符合课题解释逻辑

### Phase 2：把单区域实验做成可复现闭环

- [ ] 固化 `MDI` 正式实验口径
- [ ] 验证 identity-on 端到端稳定性
- [ ] 明确分割监督是否接入
- [ ] 生成单区域阈值校准结果
- [ ] 收口统一实验输出规范
- [ ] 补一键复现实验脚本

---

## 五、当前一句话判断

- [ ] 现在还差的不是补更多骨架，而是把**单区域真正跑通、把结果做成可解释且可复现实验输出**
