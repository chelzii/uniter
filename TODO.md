# TODO

这份 TODO 只保留**还没实现**、但和课题直接相关的内容。

当前原则：

- 现有数据里已经有的内容都要真正用上
- 不再区分“第一版 / 第二版”
- 不补保底代码，不补兼容层，不补冗余分支
- 按课题当前真实方向推进：**空间 + 意义 + 身份**
- 卫星图已在数据中存在，当前直接作为普通空间图像输入，不单开遥感分支

---

## 一、离“直接开始训练”还差什么

### 1. 跑通单区域训练主链路

- [ ] 复核当前单区域配置，删掉无意义项
  目标：
  - 只保留当前单区域训练需要的损失、指标和导出
  - 不保留对当前数据无用的开关
  - 配置和课题当前数据状态保持一致

- [ ] 跑通 `kaitong_west_lane` 首轮训练
  目标：
  - 训练命令可直接启动
  - 能稳定产出 checkpoint
  - 当前数据四类输入全部参与到应参与的流程里

- [ ] 跑通单区域评估与导出
  目标：
  - `evaluate`
  - `export-region-metrics`
  - `export-visualizations`
  - 生成可以直接看结果的输出目录

- [ ] 补 1 个最小端到端 smoke test
  位置：
  - `tests/`
  目标：
  - 覆盖 `manifest -> dataloader -> model -> trainer`
  - 只验证主链路，不做额外工程兼容

---

## 二、单区域结果还缺什么

### 1. 结果可解释

- [ ] 增加启动时数据摘要日志
  位置：
  - `src/uniter/training/trainer.py`
  目标：
  - 打印 train/val/test 样本数
  - 打印图像数、当前文本数、历史文本数、身份文本数
  - 明确当前 run 实际吃到了哪些数据

- [ ] 增加单区域结果总表
  位置：
  - `src/uniter/reporting/`
  目标：
  - 汇总 `IFI / MDI / IAI / alignment_gap / final level`
  - 直接服务案例分析，不额外做一套复杂报告系统

### 2. 结果是否符合课题解释逻辑

- [ ] 检查单区域输出是否能解释
  目标：
  - `IFI` 是否和空间图像表现一致
  - `MDI` 是否和当前/历史文本差异一致
  - `IAI` 是否能反映身份文本提供的场所角色
  - `alignment_gap` 是否只是辅助，不喧宾夺主

---

## 三、按课题方向还没完成的核心项

### 1. 多区域真实训练集

- [ ] 从单区域 bootstrap 切回真实多区域 manifest
  当前问题：
  - `data/regions.jsonl` 现在还是单区域拆出来的 bootstrap 样本
  课题要求：
  - 多个真实区域
  - 真实 `train / val / test`

- [ ] 增加多区域数据校验
  位置：
  - `src/uniter/data/manifest.py`
  目标：
  - 检查每个 split 是否有真实区域
  - 检查区域数量是否满足训练最小要求
  - 检查每个区域是否同时有空间图像、当前文本、历史文本、身份文本

### 2. 情感监督真正落地

- [ ] 接入 `sentiment_label`
  目标：
  - 让当前感知情绪不再只靠无监督表征
  - 为 `MDI` 提供更稳定的情感差异支撑

- [ ] 接入 `historical_sentiment_label`
  目标：
  - 让历史叙事与当前感知的差异不只依赖 embedding drift

- [ ] 明确 `MDI` 的正式实验口径
  目标：
  - 明确什么时候用情感漂移
  - 明确什么时候退回嵌入漂移
  - 不保留模糊表述

### 3. 身份模态完成正式化

- [ ] 增加 identity-on 的端到端测试
  目标：
  - 覆盖 identity 开启时的数据读取、前向、指标和导出

### 4. 失落空间监督头闭环

- [ ] 接入 `lost_space_label`
  目标：
  - 打开 `lost_space.enabled`
  - 训练后输出 `rule / model / fusion` 三套结论

### 5. 分割监督是否接入

- [ ] 明确空间分支是否接分割监督
  当前状态：
  - 数据里还没有 mask
  处理原则：
  - 如果后续不补 mask，就不要继续扩展这条线
  - 如果补 mask，再把 `spatial_supervision` 真正接上

### 6. 阈值校准实验闭环

- [ ] 用真实多区域样本做阈值标定
  目标：
  - 给 `IFI / MDI / IAI` 的轻中重阈值一个真实样本基线
  - 不再依赖单区域 bootstrap 的技术性占位

### 7. 论文级实验组织

- [ ] 补消融实验配置组
  目标：
  - `space only`
  - `space + meaning`
  - `space + meaning + identity`
  - `rule only`
  - `rule + learned head`

- [ ] 补统一实验输出规范
  目标：
  - checkpoint
  - metrics
  - visualizations
  - summary
  - 命名统一，不重复造轮子

- [ ] 补一键复现实验脚本
  目标：
  - 能直接跑某个配置
  - 自动导出对应结果

---

## 四、执行顺序

### Phase 0：先达到单区域可训练

- [ ] 精简当前单区域配置
- [ ] 跑通首轮训练
- [ ] 跑通评估与导出
- [ ] 补最小 smoke test

### Phase 1：把单区域结果做成可分析样本

- [ ] 增加启动数据摘要
- [ ] 生成单区域结果总表
- [ ] 检查 `IFI / MDI / IAI / alignment_gap`
- [ ] 确认单区域结果是否符合课题解释逻辑

### Phase 2：进入课题正式方向

- [ ] 接入真实多区域数据
- [ ] 接入情感标签
- [ ] 接入历史情感标签
- [ ] 接入失落空间标签
- [ ] 验证 identity-on 在正式多区域实验中的稳定性
- [ ] 做真实阈值标定
- [ ] 做消融实验

---

## 五、当前一句话判断

- [ ] 现在还差的不是补更多骨架，而是把**单区域真正跑通、把结果做成可解释输出、然后转入多区域正式实验**
